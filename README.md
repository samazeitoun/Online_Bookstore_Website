# Online-Bookstore
A PHP-based Bookstore web application running on a XAMPP server with MySQL integration. It includes user authentication (login, register, logout, checklogin), book browsing and checkout features, user profile editing, and database setup via database.sql and ConnectDB. Styled using CSS for a clean interface.
